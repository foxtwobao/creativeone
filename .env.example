APP_PORT=3001
# Use a generated URL-safe password, also used in DATABASE_URL by Compose.
POSTGRES_PASSWORD=replace-with-a-long-random-url-safe-password
# Single server edition image (frontend + API).
APP_IMAGE=your-dockerhub-username/creativeone:latest
